apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-service
  namespace: traininghub
spec:
  template:
    spec:
      containers:
        - name: frontend-service
          env:
            - name: SESSION_COOKIE_SECURE
              value: "true"
